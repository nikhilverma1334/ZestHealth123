name: patient_app
description: ZestHealth Patient App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^9.0.0
  http: ^1.1.0
  socket_io_client: ^2.0.3
  geolocator: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
